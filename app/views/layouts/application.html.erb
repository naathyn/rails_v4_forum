<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title><%= full_title(yield(:title)) %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Ruby on Rails Forum in Development" %>">
    <meta name="author" content="<%= content_for?(:author) ? yield(:author) : "Nathan Couch" %>">
      <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
      <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
      <%= csrf_meta_tags %>
      <%= render 'layouts/shim' %>
      <%= yield(:head) %>
    </head>
    <body>
      <header class="navbar navbar-inverse navbar-fixed-top">
        <nav class="navbar-inner">
          <div class="container-fluid">
          <button type="button" class="btn btn-navbar" 
                  data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
            <%= render 'layouts/navigation' %>
          </div>
        </nav>
      </header>
      <div class="page-wrapper" id="main" role="main">
        <%= render 'layouts/messages' %>
        <%= yield %>
        <%= debug(params) if Rails.env.development? %>
      </div>
      <footer class="footer">
        <div class="container">
          <%= render 'layouts/footer' %>
      </div>
    </footer>
  </body>
</html>
